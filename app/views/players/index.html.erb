<h1 class="text-primary mb-5"><%= t('.title') %></h1>

<div class="mb-4">
  <%== pagy_bootstrap_nav(@pagy) %>
</div>

<%# TODO: Revise mobile design for the leaderboard player %>
<div class="player-list">
  <% @players.each do |player| %>
    <%= render LeaderboardPlayer.new(player, 'normal') %>
  <% end %>
</div>

<div class="mt-4">
  <%== pagy_bootstrap_nav(@pagy) %>
</div>
