<h1 class="text-primary mb-4 mb-md-5"><%= t('.title') %></h1>

<div class="mb-3 mb-md-4">
  <%== pagy_bootstrap_nav(@pagy) %>
</div>

<div class="player-list">
  <% @players.each do |player| %>
    <%= render LeaderboardPlayer.new(player, 'normal') %>
  <% end %>
</div>

<div class="mt-3 mt-md-4">
  <%== pagy_bootstrap_nav(@pagy) %>
</div>
